<%= form_for(@subsection, url: copycreate_subsections_url(@subsection), html: {method: "patch"}) do |f| %>
  <% if @subsection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subsection.errors.count, "error") %> prohibited this subsection from being saved:</h2>

      <ul>
      <% @subsection.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.label :section_id %><br>
      <%= f.collection_select(:section_id, Section.where(project_id: 23), :id, :name) %>
    </div>
  <div class="field">
    <%= f.label 'Subsection Name' %><br>
    <%= f.text_field :name %><p>E.g. Bathroom</p>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
